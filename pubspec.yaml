name: my_simple_app
description: A clean Flutter ToDo app without notification plugins
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_colorpicker: ^1.1.0
  intl: ^0.20.2
  shared_preferences: ^2.3.2
  uuid: ^3.0.7
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.0.16


dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0
  build_runner: ^2.4.13
flutter:
  uses-material-design: true


